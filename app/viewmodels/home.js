define(['knockout',
        'durandal/app'
],
function (ko,
          app
          ) {
    return {
          name : ko.observable('gagan sm ')
          //showName : app.showMessage(name)
        }
});